name: Deploy Dev to Coolify

on:
  push:
    branches: [dev]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Deploy
        run: curl -fsSL "${{ secrets.COOLIFY_WEBHOOK_URL }}"
